import * as React from 'react';
import './Counter.scss';

const Counter: React.FC = () => {
  return (
    <div className="flex-row w-10/12">
      <div className="counter-title">Create Counter</div>
    </div>
  );
};

export default Counter;
